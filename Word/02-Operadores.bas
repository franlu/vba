Attribute VB_Name = "Operadores"

Sub Operadores()
'
' Ejemplos de uso de operadores basicos
'

Dim x, y As Integer
Dim n, m As Double

x = 1
y = 2
n = 3.2
m = 4.7

Selection.TypeText Text:="Variable x = " + CStr(x)
Selection.TypeParagraph

Selection.TypeText Text:="Variable n = " + CStr(n)
Selection.TypeParagraph

'suma de x e y
'Texto de ejemplo: x + y = 3
'resta de y menos x
'producto de x por y
'division de x entre y
'repetir operaciones con n y m
'Sumar x, y, n y m
'Dividir y entre n

End Sub
